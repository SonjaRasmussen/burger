<div class="container">
  <div class = "jumbotron">

      <H1> EAT DA BURGERS!</H1>
     <img src="/assets/images/burger.jpeg" class="img-fluid" alt="Responsive image">


  </div>  

<div class = "row">
    <div class="col-md-6">

        <H2>Choose a Burger</H2>
        <ul class="list-group list-group-flush">
            {{#each burgers}}
                {{#unless this.devoured}}
                <li class="list-group-item" id="burger-name">
                    {{this.burger_name}}
                        <form action="/burgers/updateOne/{{this.id}}?_method=PUT" method="POST">
                        <input type="hidden" name="devoured" value="1">
                        <button type = "submit" class="btn btn-success">Eat this one!</button>
                        </form>
                 </li>
                 {{/unless}}
                {{/each}} 
        </ul>
    </div>


<dvi class = "col-md-6">

    <H2>Ate it!</H2>
 <ul class="list-group list-group-flush">
      {{#each burgers}}
        {{#if this.devoured}}
         <li class="list-group-item" id="burger-name">
          {{this.burger_name}}
          <form action="/burgers/updateOne/{{this.id}}?_method=PUT" method="POST">
            <input type='hidden' name='devoured' value='0'>
            <button type='submit' class="btn btn-info">Ummm, No Thanks!</button>
          </form>
        </i>
        {{/if}}
      {{/each}}
    </ul>
  </div>
</div>


<div class="col-md-12 add-burger">
  <h2>Add a Burger</h2>
 
  <form class="create-form" method="POST" action="burgers/insertOne">
    <div class="form-group">
      <input type="text" id="bur" name="name">
    </div>
    <button type="submit" class="btn btn-warning">Add Burger</button>
  </form>
</div>
</div>




